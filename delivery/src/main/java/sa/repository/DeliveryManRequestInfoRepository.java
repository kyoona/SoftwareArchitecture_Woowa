package sa.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import sa.domain.DeliveryManRequestInfo;

public interface DeliveryManRequestInfoRepository extends JpaRepository<DeliveryManRequestInfo, Long>{
}
